1----改货币单位：update uap_mutil_tenancy_set SET item_value = 'R' where item_name = 'currencyUnit';
2----确保费率生效：update vd_e_bill_pkg_ver set status = '05' where status = '03';
3----更新加密服务地址：update p_third_party set interface_url = 'http://192.168.80.35:8010/EncrptionService/rest/sts' where business_type = '0402';
4----临时禁用备用盒子的运行vk：update vd_p_vk_run set status = 20 where EBOX_ID <> 3;（其实应该禁用运行加密盒，在选vkNo时过滤运行加密盒状态，最新程序已支持，请忽略）
5----customer_no极有可能改为自动生成，最好基于老系统的sequence+1，要么建立新的sequence，要么用customer_id_seq，但是要确保新的Customer_id大于老的，否则yh_sequence会撞车
6----配置操作员的有效登录时间段，绑定IP：《操作员绑定IP.xlsx》